# Backlog

1. Corrigir `.github/workflows/ci.yml` (`working-directory` e cobertura de build).
2. Adicionar `scripts/test.ps1`, `scripts/lint.ps1` etc com wrappers de projeto.
3. Criar tópico de observabilidade e rastreabilidade por run.

